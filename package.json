{
  "name": "clone-ai-os",
  "version": "1.0.0",
  "private": true,
  "description": "Clone — Production-Grade AI Employee Operating System",
  "scripts": {
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "npm --prefix apps/api run dev",
    "dev:web": "npm --prefix apps/web run dev",
    "build": "npm run build:packages && npm --prefix apps/api run build && npm --prefix apps/web run build",
    "build:api": "npm run build:packages && npm --prefix apps/api run build",
    "build:web": "npm run build:packages && npm --prefix apps/web run build",
    "start:api": "node apps/api/dist/apps/api/src/server.js",
    "build:packages": "npm --prefix packages/database run build && npm --prefix packages/integration-framework run build && npm --prefix packages/ai-runtime run build && npm --prefix packages/workspace-provider run build",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "lint": "eslint . --ext .ts,.tsx",
    "db:generate": "npm --prefix packages/database run generate",
    "db:push": "npm --prefix packages/database run db:push",
    "db:seed": "npm --prefix packages/database run seed"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0",
    "@playwright/test": "^1.44.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
